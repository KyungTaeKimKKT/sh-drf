upstream uwsgi {
    server unix:/tmp/uwsgi/shapi.sock;
}
server {
    listen      80;
    server_name mes.swgroup.co.kr;
    charset     utf-8;

    location / {
            uwsgi_pass  uwsgi;
            include     /etc/nginx/uwsgi_params;
    }
}